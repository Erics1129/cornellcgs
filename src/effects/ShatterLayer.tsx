/**
 * Glass shatter layer (§5.6) — placeholder. The full version cracks the code
 * layer like a pane of glass along the drag path, with shards, dust and shake.
 */
export default function ShatterLayer() {
  return null
}
