/**
 * Moving code background (§5.5) — placeholder. The full version is a Canvas 2D
 * layer of drifting, syntax-tinted poker/CFR code with compile flashes.
 */
export default function CodeLayer() {
  return null
}
